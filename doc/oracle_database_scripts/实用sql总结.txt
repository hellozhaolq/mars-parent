-- 修改身份证号为18位随机数，可能重复，首位可能是0
update MARS_SYS_USER t set t.id_number = substr(dbms_random.value,2,18);
-- 所以多次执行下面语句
update MARS_SYS_USER t set t.id_number = substr(dbms_random.value, 2, 18)
 where substr(t.id_number, 0, 1) = 0;
