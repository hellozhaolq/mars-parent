<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>mars-tool</artifactId>
        <groupId>com.zhaolq</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>tool-mybatis-generator</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>MyBatis Generator</description>

    <properties>
        <!-- 路径 -->
        <targetJavaProject>${project.basedir}/src/main/java</targetJavaProject>
        <targetResourcesProject>${project.basedir}/src/main/resources</targetResourcesProject>

        <!-- 包路径 -->
        <targetEntityPackage>com.zhaolq.generator.entity</targetEntityPackage>
        <targetMapperPackage>com.zhaolq.generator.mapper</targetMapperPackage>
        <targetMapperXMLPackage>mapper</targetMapperXMLPackage>

    </properties>

    <dependencies>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis-spring-boot-starter.version}</version>
        </dependency>

        <!--
        <dependency>
            <groupId>org.mybatis.generator</groupId>
            <artifactId>mybatis-generator-core</artifactId>
            <version>${mybatis-generator-core.version}</version>
        </dependency>
        -->
    </dependencies>

    <!-- 运行MyBatis Generator的几种方式 https://mybatis.org/generator/running/running.html， -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.mybatis.generator</groupId>
                <artifactId>mybatis-generator-maven-plugin</artifactId>
                <version>${mybatis-generator-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>Generate MyBatis Artifacts</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <phase>deploy</phase>
                    </execution>
                </executions>
                <configuration>
                    <!-- 定义配置文件 -->
                    <configurationFile>${project.basedir}/src/main/resources/generatorConfig.xml</configurationFile>
                    <!-- 输出详细信息 -->
                    <verbose>true</verbose>
                    <!-- 覆盖生成文件 -->
                    <overwrite>true</overwrite>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.oracle.ojdbc</groupId>
                        <artifactId>ojdbc8</artifactId>
                        <version>${ojdbc.version}</version>
                        <scope>runtime</scope>
                    </dependency>
                    <dependency>
                        <groupId>com.oracle.ojdbc</groupId>
                        <artifactId>orai18n</artifactId>
                        <version>${ojdbc.version}</version>
                        <scope>runtime</scope>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>